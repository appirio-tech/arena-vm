/*----------------------------------------------------------
LIBRARY
    sandbox

PRINT FUNCTIONS
    The print functions are used to print out the name, 
    arguments, and return values of system calls.

SAFE FUNCTIONS
    The safe functions are used to determine by the 
    arguments of the system call whether it is safe to 
    execute or not.

UTILITY FUNCTIONS
    The utility functions are mostly for reading the child
    process' memory.  Also, there is a kill funtion that
    will print a problem if the process is not killed.

----------------------------------------------------------*/
