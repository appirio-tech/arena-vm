<project name="glue-jar" xmlns:ivy="antlib:org.apache.ivy.ant" basedir=".">
    <property name="root.dir" location="${basedir}/.." />
    <property name="glue.dir" location="${root.dir}/glue" />

    <import file="${glue.dir}/glue-base.xml" />
    <import file="${glue.dir}/glue-svn.xml" />
    <import file="${glue.dir}/glue-git.xml" />
    
    <target name="compile" depends="glue-base.compile" />
</project>
